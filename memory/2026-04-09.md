# Daily Memory

- Confirmed facts:
  - Placeholder backfill created because the daily file was missing when this session started.
- Inferred:
  - None yet.
- Open gaps:
  - Need to record actual work completed in this session.

- Confirmed facts:
  - Applied OpenClaw workspace upgrade v3.1 on branch `feature/agent-improvements-v3.1`.
  - Script added SEARCH specialist, routing rules, A2A/session/developer-mode protocols, and `.upgrade_state.json` with `workspace_version=3.1`.
  - Manual follow-up corrected `MEMORY.md` domain misclassification and finished `README.md` SEARCH documentation updates.
- Inferred:
  - The original v3.1 script is usable on this repo revision, but its keyword-based MEMORY classifier is too coarse for reliable long-term use.
- Open gaps:
  - v3.2a upgrade has not been applied yet.

- Confirmed facts:
  - User explicitly set new persistent priorities by domain:
    - [JOB] prioritize applying to Agent + OR fusion roles.
    - [ENGINEER] prioritize OpenClaw work on recoverability and automatic routing implementation.

- Confirmed facts:
  - User requested a daily timed algorithm-interview crash-course push for coding-job preparation, with format: question first, then core idea, then concise Python solution.
  - User uploaded `01-算法面试速成手册.md`; content successfully loaded for review/refinement.
  - User uploaded `02-高频题型补充与易错点.md`; supplement file loaded successfully for integration.
- Confirmed facts:
  - Added `bootstrap_longclaw_m4.sh` at workspace root as a re-runnable local bootstrap script.
  - The script persists a Node 20 PATH pin into `~/.zprofile` and `~/.bash_profile`, skips already installed commands, and validates syntax with `bash -n`.
- Inferred:
  - Current Mac toolchain drift is primarily caused by mixed install states (Node 25 on PATH, missing `claude`, missing `playwright-cli`, missing Brave API key), so a repeatable bootstrap script is higher leverage than one-off repair commands.
- Confirmed facts:
  - Committed and pushed `feature/agent-improvements-v3.1` with commit `61519f7`.
  - Applied `apply_all_v3.2a.sh`, manually patched `MEMORY.md` meta history and `README.md`, then committed and pushed `feature/agent-improvements-v3.2a` with commit `c7a659e`.
  - README now documents the v3.2a workflow skill layer, progressive disclosure behavior, context compression protocol, proactive skill creation, and the local bootstrap script.
- Inferred:
  - The v3.2a script is usable but incomplete on this repo because it does not update README and its MEMORY meta anchor assumes the original v3.1 wording.
- Open gaps:
  - Local bootstrap execution was interrupted during Homebrew download and has not completed end-to-end.
- Confirmed facts:
  - Re-ran `bootstrap_longclaw_m4.sh` successfully after switching Node 20 installation from Homebrew-dependent flow to official Node tarball fallback.
  - Verified in a fresh `zsh -lc` login shell: `node v20.20.2`, `claude 2.1.97`, `playwright-cli 0.1.6`.
  - Verified `BRAVE_API_KEY` and `BRAVE_SEARCH_API_KEY` are still unset.
  - Committed local follow-up on `feature/agent-improvements-v3.2a` as `275b2c4`.
- Inferred:
  - GitHub HTTPS access is intermittently hanging from this environment; HTTPS `git push` and `git ls-remote` both stalled, and SSH fallback failed with `Permission denied (publickey)`.
- Open gaps:
  - Latest commit `275b2c4` is local-only and not yet pushed to origin.
- Confirmed facts:
  - Reviewed `feature/agent-improvements-v3.2a` for merge readiness against `main` and fixed two merge blockers before merge:
    - `bootstrap_longclaw_m4.sh` now installs Playwright skills from `$HOME` instead of the repo cwd.
    - `AGENTS.md` now states skill activation timing and compression triggers without contradictory wording.
  - Committed blocker fixes on `feature/agent-improvements-v3.2a` as `27e9c67`.
  - Merged `feature/agent-improvements-v3.2a` into local `main` with merge commit `c808e07` and no conflicts.
  - `git push origin main` still timed out after 25 seconds from this environment.
- Inferred:
  - Repository content is locally merge-ready, but remote publication remains blocked by the same outbound GitHub HTTPS stall observed earlier.
- Open gaps:
  - Local `main` is ahead of `origin/main` and still not published.
